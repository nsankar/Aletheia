# Normalize text files to LF in the repo regardless of OS checkout.
# Avoids spurious whole-file diffs from CRLF/LF across Windows/macOS/Linux contributors.
* text=auto eol=lf

# Ensure executable scripts keep their bit on all platforms.
*.py   text eol=lf
*.sh   text eol=lf
